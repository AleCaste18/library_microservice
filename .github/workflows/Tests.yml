name: Tests

on: push

jobs: 
  tests:
    name: Unit Testing
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.1.0
      - name: Test
        run: |
          cd Service/
          dotnet test 
